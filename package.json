{
  "name": "flaregun",
  "private": true,
  "description": "Self-hosted error tracking on Cloudflare. Analytics Engine only.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm --filter flaregun build && ./scripts/sync-readme.sh"
  }
}
