{
  "name": "@flaregun/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Flaregun ingest worker — receives errors and writes to Analytics Engine",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.50.0"
  }
}
